"""
Visual inspection helpers (leaf-mask QA, qualitative prediction overlays for
the report, §4.2.13).

Functions to implement:
    overlay_mask(image: numpy.ndarray, mask: numpy.ndarray, color, alpha) -> numpy.ndarray
    plot_prediction_grid(images, gt_masks, pred_masks, n=6, save_path=None) -> None
"""
